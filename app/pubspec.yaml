name: mental_ai
description: "Hearth — AI-supported mood tracking and a mental-wellness community."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.13.2

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Material/Cupertino widget translations + locale-aware date formatting,
  # so switching language also switches the framework's own strings and the
  # way dates are written (not just our labels).
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1
  # Declarative routing
  go_router: ^14.6.2
  # HTTP client for the Rust backend API
  dio: ^5.7.0
  # Local key-value storage (settings, cached user id)
  shared_preferences: ^2.3.3
  # date/time formatting for reports and mood history
  intl: ^0.20.2
  # generates the local, non-authenticating device/user id
  uuid: ^4.5.1
  # dials 112 directly from the chat crisis banner
  url_launcher: ^6.3.1
  image_picker: ^1.2.3
  http_parser: ^4.1.2
  # Push notifications: FCM delivery plus local display while foregrounded
  # (Android/iOS don't auto-show a "notification"-type FCM message when
  # the app is already open — that's the whole point of onMessage).
  firebase_core: ^3.8.0
  firebase_messaging: ^15.1.6
  flutter_local_notifications: ^18.0.1
  # Renders the weekly recap card to an image and hands it to the native
  # share sheet (via an in-memory `XFile`, so no temp-file/path_provider
  # plumbing is needed and this works on web too).
  share_plus: ^10.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # Builds AppLocalizations from lib/l10n/*.arb (see l10n.yaml).
  generate: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Instrument Sans (light theme) and Sora (dark theme) — see
  # `app/theme/app_theme.dart`. Bundled as variable-font assets rather
  # than fetched at runtime (e.g. via a `google_fonts`-style package) so
  # text renders correctly offline and on first launch, which matters
  # more here than in a typical app: this is a local-first product whose
  # backend may not even be reachable yet when the login screen first
  # paints.
  fonts:
    - family: Instrument Sans
      fonts:
        - asset: assets/fonts/InstrumentSans-Variable.ttf
    - family: Sora
      fonts:
        - asset: assets/fonts/Sora-Variable.ttf

# Source art for the app icon lives in assets/icon/ — `icon.png` is the mark
# on the light-theme canvas color (used as-is for iOS/web/legacy Android),
# `icon_foreground.png` is the same mark alone on a transparent background,
# scaled into Android's adaptive-icon safe zone so it isn't clipped by the
# OS's own circular/squircle mask. Regenerate with:
#   dart run flutter_launcher_icons
flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/icon/icon.png"
  min_sdk_android: 21
  adaptive_icon_background: "#F5F6F2"
  adaptive_icon_foreground: "assets/icon/icon_foreground.png"
  web:
    generate: true
    image_path: "assets/icon/icon.png"
    background_color: "#F5F6F2"
    theme_color: "#5E7A57"
