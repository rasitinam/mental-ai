<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<!-- Replace with your Apple Developer Team ID (Apple Developer
	     portal → Membership). Placeholder on purpose — this file is
	     committed since it carries no secret, only your public team ID. -->
	<key>teamID</key>
	<string>REPLACE_WITH_YOUR_APPLE_TEAM_ID</string>
	<!-- "manual" because the GitHub Actions workflow (release-ios.yml)
	     signs with a specific imported certificate + provisioning
	     profile, not a logged-in Apple ID — "automatic" made the export
	     step fail with "No Accounts" / "No profiles ... were found" even
	     after the archive itself was signed correctly. If you wire up
	     Codemagic (see ../../codemagic.yaml) instead, its own
	     `xcode-project use-profiles` step can override this back to
	     automatic at build time. -->
	<key>signingStyle</key>
	<string>manual</string>
	<key>provisioningProfiles</key>
	<dict>
		<key>com.rasitinam.hearth</key>
		<string>REPLACE_WITH_PROVISIONING_PROFILE_UUID</string>
	</dict>
	<key>uploadBitcode</key>
	<false/>
	<key>uploadSymbols</key>
	<true/>
</dict>
</plist>
