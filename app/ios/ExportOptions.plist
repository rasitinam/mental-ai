<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>method</key>
	<string>app-store-connect</string>
	<!-- Replace with your Apple Developer Team ID (Apple Developer
	     portal → Membership). Placeholder on purpose — this file is
	     committed since it carries no secret, only your public team ID. -->
	<key>teamID</key>
	<string>REPLACE_WITH_YOUR_APPLE_TEAM_ID</string>
	<!-- "automatic" so Codemagic's `xcode-project use-profiles` step (see
	     ../../codemagic.yaml) can fetch/generate the right provisioning
	     profile itself via its App Store Connect API key integration,
	     rather than needing a manually-created and -downloaded profile
	     from someone sitting at a Mac. -->
	<key>signingStyle</key>
	<string>automatic</string>
	<key>uploadBitcode</key>
	<false/>
	<key>uploadSymbols</key>
	<true/>
</dict>
</plist>
